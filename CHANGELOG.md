# 更新日志 (CHANGELOG)

## [2.0.1] - 2026-05-05

### 🛠️ 工业级加固 (Industrial Grade Hardening)
- **LaTeX 掩码保护机制**: 引入多级掩码机制，彻底解决 HTML 标签与 LaTeX 语法冲突问题。现在支持在 HTML 中完美渲染包含 `<`、`>` 等特殊符号的复杂数学公式，不再会被 HTML 解析器误删或破坏。
- **长报告截断修复**: 将格式清洗层的上下文窗口提升至 15,000 字符，并优化了输入截断逻辑，确保长篇学术报告能够完整生成而不被腰斩。
- **MathJax 3 深度优化**: 升级渲染配置，引入 `window.MATH_DONE` 状态同步机制。Playwright 会严格等待 MathJax 渲染完成后再执行 PDF 打印，杜绝公式乱码和白屏。
- **学术级 CSS 样式升级**: 重新设计了 PDF 内置 CSS，增强了定理块 (`.theorem`)、证明块 (`.proof`) 的视觉表现，使其更符合学术论文规范。

### 🐞 修复 (Fix)
- 修复了 LaTeX 内部特殊字符（如 `&`）破坏 HTML 结构的底层 Bug。
- 优化了 OCR 识别后的文本清洗逻辑，减少了冗余的 Markdown 标记对 PDF 渲染的干扰。

## [1.3.1] - 2026-05-04

### 🚀 新增功能
- **多级备用模型逻辑 (Fallback Mechanism)**:
  - 在配置中新增 `fallback_vision_models`，支持视觉识别 (OCR) 阶段的多模型降级。
  - 完善文本逻辑模型的备用机制，主模型异常时自动按序列切换。
  - 增加模型切换时的实时消息提示，提升交互透明度。

### 🛠️ 优化
- 优化了 `_conf_schema.json` 中的描述文字，使其更易于理解。
- 移除了冗余的内部状态变量，代码结构更简洁。

## [2.0.0] - 2026-04-26
### 重大更新 (Major Update)
- **图片处理架构重构**：彻底修复了 OCR 识别中"无法访问外部 URL"的问题。现在所有图片（包括 `http://`、`file://` 等协议）都会自动转换为 base64 格式后再发送给视觉模型，确保 100% 的识别成功率。
- **PDF 引用功能完善**：支持引用历史 PDF 文件并自动进行内容提取。系统会下载远程 PDF，转换为高清图片，然后通过 OCR 识别所有内容（包括数学公式的 LaTeX 格式）。
- **自动清理机制**：新增定时清理任务，每天凌晨 3:00 自动清理超过 24 小时的临时文件，有效节省存储空间。

### 新增功能 (Features)
- **启动时清理**：插件启动后 10 秒自动执行首次清理任务
- **清理范围扩展**：
  - `/AstrBot/data/pdf_reports/` 下的所有 PDF 和 PNG 文件
  - `/tmp/` 下以 `remote_` 或 `remote_img_` 开头的临时文件
- **详细清理日志**：记录每个被删除文件的名称、年龄和总删除数量

### 优化 (Optimization)
- **日志增强**：添加完整的输入内容日志，包括 OCR 识别结果和用户指令
- **图片处理日志**：记录图片 URL 转换过程和 base64 长度，便于调试
- **PDF 生成优化**：修复了 `plain_result()` 方法的参数错误，确保 PDF 能正常生成和发送

### 修复 (Fix)
- **OCR 识别失败**：修复了图片 URL 无法被视觉模型访问的问题
- **PDF 发送失败**：修复了 `message_chain` 参数错误导致的 PDF 生成崩溃
- **多页 PDF 处理**：优化了 PDF 转图片的流程，支持正确处理多页文档

## [1.8.6] - 2026-04-17
### 修复 (Fix)
- **视觉调用崩溃问题**：修复了在此前移除通用变量时，意外造成 `vision_prompt` 未定义导致的 `NameError`。现在 OCR 请求已恢复正常的提取引导功能。


## [1.8.5] - 2026-04-17
### 特性 (Feature)
- **多供应商 API 完全隔离**：现在支持为"逻辑大脑"和"视觉 OCR"分别配置独立的 API Key。这使得用户可以根据需求，灵活组合不同服务商的能力（例如使用 DeepSeek 进行逻辑推理，同时使用阿里 Qwen 进行高精度图像解析）。


## [1.8.4] - 2026-04-17
### 优化 (Optimization)
- **配置界面 UI 极简化**：根据主人反馈，移除了冗长的 `llm_base_url` 通用配置项。现在配置界面仅保留两路明确的 API URL：逻辑大脑（文本）与视觉解析（OCR），大幅降低填写难度并提升了路由逻辑的可读性。


## [1.8.3] - 2026-04-17
### 特性 (Feature)
- **同步 Cline 代码修改**：整合了最新的多模态解析逻辑与配置定义。
- **配置架构优化**：优化了 `_conf_schema.json` 中的字段描述与校验规则。


## [1.8.2] - 2026-04-17
### 特性 (Feature)
- **支持引用消息的文本跨时空理解**：大幅强化对于 Reply 引用组件的底层解析架构。在保留原有提取图片的能力外，现已支持全面深度抓取引用的全部历史文字上下文。通过在 Prompt 中对 `【被引用的历史上下文】` 与 `【用户的当前指令】` 进行严格分区，使大模型完美具备了能够同时"看图看上下文"甚至直接回答"翻译这段话"的能力。

## [1.8.1] - 2026-04-16
### 修复
- **历史引用图片提取失效**：修复了在回复（Quote/Reply）包含图片的旧消息时，由于 OneBot/NapCat 的 `get_msg` 接口返回降级后的 CQ 码字符串（而非标准 JSON 数组）导致的正则解析忽略问题。现已引入"穿透取值 + CQ码提取"双引擎兜底解析，完美支持历史引用图片的学术渲染。

## [1.8.0] - 2026-04-16
### 重大变更
- **彻底取消闲聊功能**：应主人要求，插件现在彻底移除了直接文本回复的 `chat` 模式。无论用户是发送"你好"还是复杂的公式，所有内容都将被强制重定向至 PDF 渲染引擎进行学术化排版交付。

### 优化
- **鲁棒路由控制**：在逻辑大脑层面显式禁用了闲聊分支，并增加了"Chat to PDF"的自动转换层，确保所有 AI 响应流最终都能以文件形式到达用户终端。
- **系统提示词重构**：重写了 `system_prompt.txt`，将所有输出模式统一为单一的 PDF 交付模式。

## [1.7.3] - 2026-04-16
### 修复
- **Pydantic 类型校验崩溃**：`chat_messages` 列表中的元素在喂给 `event.plain_result()` 前，现在强制执行 `str()` 类型转换，彻底防止 LLM 返回了 `null` / 数字等非字符串类型导致的 Pydantic `ValidationError` 崩溃。
- **视觉 API 失败后的静默崩溃**：之前视觉 API 三次重试全部失败时，代码会带着空的 `image_description` 继续调用文本模型，并最终因 LLM 返回格式异常而 crash。现在已在重试耗尽时主动向的提示信息，并优雅地降级为纯文本问答，不再崩溃。

## [1.7.2] - 2026-04-16
### 修复
- **PDF 渲染超时问题锁定**：重构了 Playwright 对 MathJax 的生命周期监听逻辑。替换 jsdelivr 为访问稳定的大陆极速镜像 `elemecdn`，并且将原本脆弱的属性探测改为了原生的 Promise `pageReady` 钩子回调（`window.MATHJAX_DONE`）。这一组合彻底根治了偶发的 30 秒等待超时渲染崩溃问题。

## [1.7.1] - 2026-04-16
### 修复
- **区块级公式不渲染问题**：通过向 MathJax 运行时显式注入 `displayMath: [['$$','$$']]` 与 `inlineMath: [['$','$']]` 配置，修复了默认情况下 MathJax v3 无法识别通过 `$$` 包裹 LaTeX 进行分行渲染的 Bug。

## [1.7.0] - 2026-04-16
### 重构
- **PDF 渲染引擎现代化**：彻底移除对过时且维护停滞的 `wkhtmltopdf` (`pdfkit`) 的依赖，全面升级为基于 `Playwright` 的全异步浏览器渲染架构。显著提升了排版美观度、生成稳定性和渲染速度。
：新架构原生完美支持苹果 M 系列 (Apple Silicon) 芯片，直接内置于 Python 环境，解决了此前复杂的自建依赖安装流程。

### 修复
- **公式渲染时序修正**：针对包含 LaTeX 数学公式的复杂解答，由于 Playwright 处理过快，引入了 `typesetPromise` 拦截阀。现在程序强制等待客户端的 MathJax 引擎渲染完所有前端 DOM 后才开始生成无头快照，彻底根治了导出 PDF 文件中出现原始 LaTeX 文本乱码的痛点。
- **弱链接剥离**：清理了代码内部失效的第三方 polyfill.io 脚本节点，提升页面挂载和导出响应速度。

## [1.6.10] - 2026-04-15
### 新增
- **兼容性大一统 (Greedy Match)**：实现了针对 OneBot/NapCat 协议的"多跳"图片匹配逻辑。不仅搜索标准 `url` 字段，还增加了对 `file`, `path` 等非标字段的贪婪兼容。
- **本地路径识别**：增加了对物理路径自动转 `file://` 协议的支持，大幅提升了在 NapCat 环境下的图片提取稳定性。

## [1.6.7] - 2026-04-15
### 修复
- **消息 ID 捕获深度适配**：针对 v4.23.1 中 `Reply` 组件字段名的隐蔽性，增加了对 `start_id` 和 `id` 字段的双重兼容探测。现在可以更稳定地从被回复的历史消息中精准"钓"出图片了。

## [1.6.6] - 2026-04-15
### 修复
- **API 兼容性终极修正**：将错误的 `get_adapter` 调用更正为 v4.23.1 官方支持的 `get_platform_inst`，彻底解决了回复图片提取时的崩溃问题。

## [1.6.5] - 2026-04-15
### 修复
- **历史图片提取修复**：修复了在"回复"消息时，由于旧版 AstrBot `event` 对象缺少 `adapter` 属性导致的图片提取失败 Bug。现在通过 `self.context` 正确调度适配器。

## [1.6.1] - 2026-04-15
### 修复
- **变量定义热修复**：修复了视觉中转链路中 `headers` 变量未定义的 Bug。
- **稳定性增强**：清理了冗余的异常捕获逻辑。

## [1.6.0] - 2026-04-15
### 新增
- **视觉中转链路 (Vision Relay Chain)**：重构了多模态处理逻辑。现在视觉模型（如 Qwen-VL）仅负责纯粹的 OCR 提取，而精细的逻辑分析与 PDF 调度则交由逻辑更强的文本模型（如 DeepSeek）处理。这种"分工协作"模式显著降低了视觉模型的压力并提升了回答质量。

## [1.5.2] - 2026-04-15
### 优化
- **视觉感知增强 (OCR Prompt)**：重构了多模态模式下的 System Prompt，显式指示模型在看到图片时启动高精度文字/公式/图表识别，消除逻辑漂移。

## [1.5.1] - 2026-04-15
### 优化
- **429 错误友好提示**：专门捕获由于 API 提供商频率限制导致的 429 错误，并给出直观的中文字样建议。

## [1.5.0] - 2026-04-15
### 新增
- **多模态智能分流引擎**：自动识别消息内容。纯文本调用 `llm_model`（如 DeepSeek），带图片调用 `llm_vision_model`（如 Qwen-VL）。
- **增强配置项**：在管理页面新增了视觉模型（Vision Model）的独立配置通道。
### 修复
- 解决了纯文本模型接收图片数据导致 API 报错 400 的物理限制。

## [1.4.2] - 2026-04-15
### 优化
- **极简说明书**：遵循简洁原则，将帮助信息重构为纯干货指令清单。

## 2026-04-15
### 新增
- **交互式帮助手册**：支持通过 `/ai help` 或发送空指令自动触发功能介绍。

## [1.4.0] - 2026-04-15
### 重构
- **大脑集成**：彻底移除对外部 8000 端口服务的依赖，将 LLM 路由逻辑直接内置于插件中。
- **可视化配置系统**：新增 `_conf_schema.json`，支持在 AstrBot WebUI 直接填写 API Key 和 URL。

## [1.3.3] - 2026-04-15
### 修复
- **API 标准化**：将 `await event.reply` 全部替换为官方规范的 `yield event.plain_result`。

## [1.3.2] - 2026-04-15
### 修复
- **属性路径兼容**：增加对旧版 AstrBot 消息对象 `.message` 属性路径的支持（适配 v4.23.1）。

## [1.3.1] - 2026-04-15
### 修复
- **版本探测与降级**：通过运行环境嗅探确认版本 API，锁定使用最稳健的 `@filter.command` 触发模式。

## [1.2.x] - 2- 初始重构与 Git 仓库初始化。
